// Stub for streamquality_test.cpp -- the two enums moonlight's own decoder.h needs to parse.
#pragma once
class StreamingPreferences {
public: enum VideoDecoderSelection { VDS_AUTO }; enum RendererSelection { RS_AUTO };
};
